"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import Link from "next/link";
import { CATEGORIES } from "@/lib/types";
import { saveProfile } from "@/lib/mock-data";

export default function OnboardingPage() {
  const router = useRouter(); const [form, setForm] = useState({ email: "", name: "", role: "frontend developer", skills: "React, TypeScript, Python", location: "Nigeria", remoteOnly: true, experienceLevel: "student", opportunityTypes: ["internship", "hackathon", "scholarship"] });
  const toggleType = (type: string) => setForm((current) => ({ ...current, opportunityTypes: current.opportunityTypes.includes(type) ? current.opportunityTypes.filter((item) => item !== type) : [...current.opportunityTypes, type] }));
  const submit = (event: React.FormEvent) => { event.preventDefault(); saveProfile({ ...form, skills: form.skills.split(",").map((skill) => skill.trim()).filter(Boolean) }); router.push("/dashboard"); };
  return <main className="min-h-screen bg-slate-50"><div className="mx-auto max-w-5xl px-5 py-8 sm:px-6"><Link href="/" className="inline-flex items-center gap-2 text-sm font-semibold text-slate-500 hover:text-indigo-600">← Opportunity Radar</Link><div className="mt-10 grid gap-10 lg:grid-cols-[.8fr_1.2fr]"><aside className="lg:pt-8"><p className="eyebrow">Build your Radar</p><h1 className="mt-3 text-4xl font-bold tracking-tight">Let&apos;s find the opportunities meant for you.</h1><p className="mt-4 max-w-sm leading-7 text-slate-600">A thoughtful profile produces better matches and clearer reasons to act.</p><div className="mt-9 space-y-5 border-l border-indigo-100 pl-5"><p className="text-sm font-semibold text-indigo-700">01 <span className="ml-3 text-slate-900">Your background</span></p><p className="text-sm font-semibold text-indigo-700">02 <span className="ml-3 text-slate-900">Your preferences</span></p><p className="text-sm font-medium text-slate-400">03 <span className="ml-3">Your opportunity list</span></p></div></aside><form onSubmit={submit} className="surface p-6 sm:p-8"><div><h2 className="text-xl font-bold">Your opportunity profile</h2><p className="mt-1 text-sm text-slate-500">Saved in this browser for the frontend demo.</p></div><div className="mt-7 grid gap-5 sm:grid-cols-2"><label className="block"><span className="text-sm font-semibold">Email address</span><input required type="email" className="field" placeholder="you@example.com" value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} /></label><label className="block"><span className="text-sm font-semibold">Name <span className="font-normal text-slate-400">(optional)</span></span><input className="field" placeholder="Your name" value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} /></label></div><label className="mt-5 block"><span className="text-sm font-semibold">What do you do?</span><input className="field" value={form.role} onChange={(e) => setForm({ ...form, role: e.target.value })} /></label><label className="mt-5 block"><span className="text-sm font-semibold">Skills</span><input className="field" value={form.skills} onChange={(e) => setForm({ ...form, skills: e.target.value })} /><span className="mt-1.5 block text-xs text-slate-400">Separate skills with commas</span></label><div className="mt-5 grid gap-5 sm:grid-cols-2"><label className="block"><span className="text-sm font-semibold">Based in</span><input className="field" value={form.location} onChange={(e) => setForm({ ...form, location: e.target.value })} /></label><label className="block"><span className="text-sm font-semibold">Experience level</span><select className="field" value={form.experienceLevel} onChange={(e) => setForm({ ...form, experienceLevel: e.target.value })}><option value="student">Student</option><option value="junior">Junior</option><option value="mid">Mid-level</option><option value="senior">Senior</option></select></label></div><div className="mt-6"><p className="text-sm font-semibold">What are you looking for?</p><div className="mt-3 flex flex-wrap gap-2">{CATEGORIES.map((category) => <button type="button" key={category} onClick={() => toggleType(category)} className={`rounded-full border px-3 py-1.5 text-sm font-medium capitalize transition ${form.opportunityTypes.includes(category) ? "border-indigo-600 bg-indigo-600 text-white" : "border-slate-200 bg-white text-slate-600 hover:border-indigo-300"}`}>{category}</button>)}</div></div><label className="mt-6 flex cursor-pointer items-center gap-3 rounded-xl bg-slate-50 p-3.5 text-sm font-medium text-slate-700"><input type="checkbox" className="h-4 w-4 accent-indigo-600" checked={form.remoteOnly} onChange={(e) => setForm({ ...form, remoteOnly: e.target.checked })} />Only show remote opportunities</label><button className="button-primary mt-7 w-full">Create my Radar →</button></form></div></div></main>;
}
